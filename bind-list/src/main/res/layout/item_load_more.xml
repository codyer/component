<!--
  ~ *************************************************************
  ~ 文件：item_load_more.xml  模块：bind-list  项目：component
  ~ 当前修改时间：2019年04月12日 09:21:19
  ~ 上次修改时间：2019年04月09日 15:30:56
  ~ 作者：Cody.yi   https://github.com/codyer
  ~
  ~ 描述：bind-list
  ~ Copyright (c) 2019
  ~ *************************************************************
  -->

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="onClickListener"
            type="android.view.View.OnClickListener" />

        <variable
            name="viewData"
            type="com.cody.component.list.data.ItemMultiViewData" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="@dimen/padding">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="horizontal"
            android:visibility="@{(viewData.noMoreItem || viewData.error)?View.VISIBLE:View.GONE}"
            android:weightSum="4"
            tools:visibility="visible">

            <View
                android:layout_width="0dp"
                android:layout_height="@dimen/line_height"
                android:layout_weight="1"
                android:background="@color/line" />

            <TextView
                android:id="@+id/loadMoreInfo"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:paddingLeft="@dimen/padding"
                android:paddingRight="@dimen/padding"
                android:text="@{viewData.error?viewData.errorMessage:@string/no_more_data}"
                tools:text="@string/no_more_data" />

            <View
                android:layout_width="0dp"
                android:layout_height="@dimen/line_height"
                android:layout_weight="1"
                android:background="@color/line" />

        </LinearLayout>

        <ProgressBar
            android:id="@+id/progressBar"
            style="?android:attr/progressBarStyle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="@{viewData.loading?View.VISIBLE:View.GONE}"
            tools:visibility="visible" />

        <Button
            android:id="@+id/retryButton"
            style="@style/Widget.AppCompat.Button.Colored"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onClick="@{onClickListener::onClick}"
            android:text="@string/retry"
            android:visibility="@{viewData.error?View.VISIBLE:View.GONE}"
            tools:visibility="visible" />

    </LinearLayout>
</layout>